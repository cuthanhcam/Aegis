# Copy these values into your shell or into a local .env file at the repository root.
# Do not commit real local secrets.

POSTGRES_PASSWORD=postgres
POSTGRES_PORT=55432
REDIS_PORT=6379
JWT_SECRET=replace-with-a-local-dev-secret-at-least-32-characters
AEGIS_DEMO_ADMIN_PASSWORD=admin-dev-password
AEGIS_DEMO_DEV_PASSWORD=dev-password
